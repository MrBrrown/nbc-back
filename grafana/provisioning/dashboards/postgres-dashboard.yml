apiVersion: 1

providers:
- name: 'Postgres-Dashboard' # Имя может быть любым, но должно быть уникальным
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  editable: true
  options:
    path: /etc/grafana/provisioning/dashboards/postgres-dashboard.json